# openterface-rs CLI — help snapshots (W1.4 contract)
#
# Generated from `openterface-rs <cmd> --help`. This is the parity contract
# with the C++ Openterface CLI; regenerate if the surface changes.

$ openterface-rs --help
Openterface USB KVM CLI — control a target's keyboard, video, and mouse over USB.

Usage: openterface-rs [OPTIONS] <COMMAND>

Commands:
  connect  Connect to KVM device (auto-discovers devices if none specified)
  scan     Scan for Openterface devices
  status   Show device status
  reset    Perform factory reset of CH9329 chip
  help     Print this message or the help of the given subcommand(s)

Options:
  -v, --verbose  Enable verbose output
  -h, --help     Print help
  -V, --version  Print version

$ openterface-rs --version
openterface-rs 0.0.0

$ openterface-rs connect --help
Connect to KVM device (auto-discovers devices if none specified)

Usage: openterface-rs connect [OPTIONS]

Options:
  -v, --verbose        Enable verbose output
      --video <PATH>   Video device path (optional - auto-detected if omitted)
      --serial <PATH>  Serial device path (optional - auto-detected if omitted)
      --no-video       Disable video capture (even if device detected)
      --no-serial      Disable input forwarding (even if device detected)
      --dummy          Run in dummy mode (no device connection, GUI only)
      --debug          Enable debug output for input events (mouse/keyboard)
  -h, --help           Print help
  -V, --version        Print version

$ openterface-rs scan --help
Scan for Openterface devices

Usage: openterface-rs scan [OPTIONS]

Options:
  -v, --verbose  Enable verbose output
  -h, --help     Print help
  -V, --version  Print version

$ openterface-rs status --help
Show device status

Usage: openterface-rs status [OPTIONS]

Options:
  -v, --verbose  Enable verbose output
  -h, --help     Print help
  -V, --version  Print version

$ openterface-rs reset --help
Perform factory reset of CH9329 chip

Usage: openterface-rs reset [OPTIONS]

Options:
      --serial <PATH>
          Serial device path (required for reset).
          
          Kept optional at the parser level to match the C++ CLI, which validates presence in the handler and prints usage; W4.1 wires that behavior.

  -v, --verbose
          Enable verbose output

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
